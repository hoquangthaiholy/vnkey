[workspace]
members = [
    "vnkey-engine",
    "vnkey-daemon",
    "vnkey-ui/src-tauri"
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

